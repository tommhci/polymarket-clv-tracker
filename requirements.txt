# Polymarket edge scanner — Python dependencies
# Install: pip install -r requirements.txt

requests>=2.31.0       # HTTP calls (Gamma API, CLOB, Odds API, Telegram)
python-dotenv>=1.0.0   # Load .env file into os.environ
openai>=1.0.0          # GLM API client (OpenAI-compatible, same SDK)
tavily-python>=0.5.0   # LLM-optimised news search (free 1000/month)
